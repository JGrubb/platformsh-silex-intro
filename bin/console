#!/usr/bin/env php
<?php
// application.php

require __DIR__.'/../vendor/autoload.php';
define('APP_ROOT', realpath(__DIR__ . '/../'));
use Symfony\Component\Console\Application;

$app = new Application();

$app->add(new \Ibd\Command\TestCommand());
$app->add(new \Ibd\Command\Post\ListPostsCommand());

$app->run();